"""Stub ``homeassistant.helpers`` package (``event`` / ``entity`` only).

The registries (``area_registry`` / ``device_registry`` / ``entity_registry``)
are intentionally absent — ``casasmart.filtering`` and the view layer need
real HA, and their container-only suites must keep skipping here.
"""
